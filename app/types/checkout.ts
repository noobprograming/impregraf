export enum PayType {
	Total = "total",
	Sena = "sena",
}

export enum PurchaseOrderStatus {
	Paid = "paid",
	Pending = "pending",
	Failed = "failed",
}

export enum Delivery {
	Local = "local",
	Delivery = "delivery",
}

export enum AmountType {
	Total = "total",
	Sena = "sena",
}
